" Initializing {{{1
scriptencoding utf-8

augroup vimrc
    autocmd!
augroup END

filetype plugin indent on

syntax enable

" Japanese {{{1
if ! has('gui_running')
    let &termencoding = &encoding
endif

set encoding=utf-8

set fileencodings=ucs-bom,utf-8
set fileencodings+=iso-2022-jp-3,iso-2022-jp
set fileencodings+=euc-jisx0213,euc-jp
set fileencodings+=cp932,shift_jisx0213,shift_jis

if exists('+spelllang')
    set spelllang&
    set spelllang+=cjk
endif

" System {{{1
let s:user_dir = expand('~/.vim')

if has('win32') " {{{2
    let s:user_dir = expand('~/vimfiles')

    autocmd vimrc BufNewFile * setlocal fileencoding=cp932 fileformat=dos

    set ambiwidth=double

    source $VIMRUNTIME/delmenu.vim
    set langmenu=none
    source $VIMRUNTIME/menu.vim

    if ! has('gui_running')
        set t_Co=256

        if exists('+termguicolors')
            set termguicolors
        endif
    endif
elseif has('macunix') " {{{2
    if exists('+macmeta')
        set macmeta
    endif
endif

" Interface {{{1
set clipboard&
if has('unnamedplus')
    set clipboard+=unnnamedplus
else
    set clipboard+=unnamed
endif

set cmdheight=2

if exists('+diffopt') && version > 801
    set diffopt&
    set diffopt+=vertical
endif

set display&
set display+=lastline

if exists('+foldmethod')
    set foldmethod=marker
endif

if exists('+guioptions')
    set guioptions&
    set guioptions-=e
    set guioptions-=m
    set guioptions-=T
    set guioptions+=c
endif

set hidden

set history=10000

set laststatus=2

set lazyredraw

set list

if &encoding == 'utf-8' && ! has('win32')
    set listchars=tab:›\ ,trail:∙,extends:»,precedes:«
else
    set listchars=tab:>\ ,trail:-,extends:>,precedes:<
endif

set number

if exists('+ruler')
    set ruler
endif

set scrolloff=5

set shortmess&
set shortmess+=aI

set sidescrolloff=5

if exists('+signcolumn')
    set signcolumn=yes
endif

if exists('+splitbelow')
    set splitbelow
endif

if exists('+splitright')
    set splitright
endif

if exists('+statusline')
    set statusline=%<%f\ %m%r%=
    set statusline+=%{'['.(&fileencoding!=''?&fileencoding:&encoding).']['.&fileformat.']'}
    set statusline+=%y\ %p\ %l:%v
endif

if exists('+tabpagemax')
    set tabpagemax=50
endif

if exists('+virtualedit')
    set virtualedit=block
endif

set visualbell t_vb=

set whichwrap&
set whichwrap+=<,>,~,[,]

if exists('+wildmenu')
    set wildmenu
endif

" Editing {{{1
set autoindent

set autoread

set backspace=indent,eol,start

set nobackup

if exists('+completeopt')
    set completeopt=menu,longest
endif

set expandtab

set formatoptions&
set formatoptions-=t
set formatoptions-=c
set formatoptions+=jmM

set nrformats&
set nrformats-=octal

if exists('+smartindent')
    set smartindent
endif

set smarttab

set softtabstop=4

set shiftwidth=4

set noswapfile

set tabstop=4

if exists('+undofile')
    let &undodir = s:user_dir . '/undo'

    if exists('*mkdir')
        call mkdir(&undodir, 'p', '0700')
    endif

    set undofile
endif

if exists('+viminfo')
    let &viminfo = &viminfo . ',n' . s:user_dir . '/viminfo'
endif

set wildmode=list:longest,full

set nowritebackup

" Search {{{1
if executable('rg')
    set grepprg=rg\ --vimgrep

    set grepformat&
    set grepformat+=%f:%l:%c:%m
elseif executable('ag')
    set grepprg=ag\ --vimgrep

    set grepformat&
    set grepformat+=%f:%l:%c:%m
endif

command! -nargs=+ Grep silent grep! <args> | copen

if exists('+hlsearch')
    set hlsearch
endif

set ignorecase

if exists('+incsearch')
    set incsearch
endif

set smartcase

" Printing {{{1
if exists('+printheader')
    set printheader=%{expand(\"%:t:r\")}%=Page\ %N
endif

" Keymapping {{{1
let mapleader = ','
let maplocalleader = ' '

noremap <F1> <Esc>
noremap! <F1> <Esc>

noremap <silent> <Esc><Esc> :nohlsearch<CR>

noremap <M-j> <Down>
noremap! <M-j> <Down>
noremap <M-k> <Up>
noremap! <M-k> <Up>
noremap <M-h> <Left>
noremap! <M-h> <Left>
noremap <M-l> <Right>
noremap! <M-l> <Right>

noremap n nzz
noremap N Nzz

noremap <silent> <M-t> :tabnew<CR>
noremap! <silent> <M-t> <Esc>:tabnew<CR>
noremap <silent> <Tab> :tabnext<CR>
noremap <silent> <S-Tab> :tabprevious<CR>

noremap <C-w>\| :vsplit<CR>
noremap <C-w>- :split<CR>

noremap <silent> <Leader>q :lclose \| copen<CR>
noremap <silent> <Leader>l :cclose \| silent! lopen<CR>

if has('terminal')
    noremap <silent> <Leader>sh :vsplit +terminal\ ++curwin<CR>
endif

if exists(':Grep')
    nnoremap <silent> <Leader>g :execute 'Grep ' . expand('<cword>')<CR>
    vnoremap <silent> <Leader>g "vy:execute 'Grep ' . @v<CR>
endif

" Filetypes {{{1
augroup vimrc
    autocmd FileType help,netrw,qf noremap <buffer> q <C-w>c
    autocmd FileType qf nnoremap <buffer> t <C-w><Enter><C-w>T
augroup END

" Utilities {{{1
command! -nargs=? -complete=dir Cd  call s:change_directory('<args>') " {{{2

function s:change_directory(directory)
    if a:directory == ''
        lcd %:p:h
    else
        execute('lcd ' . a:directory)
    endif
endfunction

if has('macunix') && executable('open') " {{{2
    command! -nargs=1 MacDictLookup call s:mac_dict_lookup(expand('<args>'))

    function! s:mac_dict_lookup(word)
        call system('open ' . shellescape('dict://' . a:word))
    endfunction

    nnoremap <silent> <LocalLeader>d :MacDictLookup <cword><CR>
    vnoremap <silent> <LocalLeader>d "vy:MacDictLookup <C-r>v<CR>
endif

" Packages {{{1
if ! filereadable(expand(s:user_dir . '/autoload/plug.vim')) && executable('curl') " {{{2
    let s:vimplug_url = 'https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim'
    silent execute '! curl -fLo ' . expand(s:user_dir . '/autoload/plug.vim') . ' --create-dirs ' . s:vimplug_url
    autocmd vimrc VimEnter *
                \  PlugInstall --sync
                \| if filereadable(expand(s:user_dir . '/coc-install.vim'))
                \|     execute 'source ' . expand(s:user_dir . '/coc-install.vim')
                \| endif
                \| quitall
endif

if filereadable(expand(s:user_dir . '/autoload/plug.vim')) " {{{2
    function! s:is_plugged(name)
        return exists('g:plugs') && has_key(g:plugs, a:name) && has_key(g:plugs[a:name], 'dir')
                    \ ? isdirectory(g:plugs[a:name]['dir'])
                    \ : 0
    endfunction

    " Plug {{{2
    call plug#begin(s:user_dir . '/plugged')

    Plug 'rafi/awesome-vim-colorschemes'
    if executable('yarn')
        Plug 'neoclide/coc.nvim', { 'do': 'yarn install --frozen-lockfile' }
        Plug 'iamcco/markdown-preview.nvim', { 'do': 'cd app && yarn install' }
    endif
    if executable('fzf')
        Plug 'junegunn/fzf', { 'do': './install --all' }
        Plug 'junegunn/fzf.vim'
    endif
    Plug 'preservim/nerdcommenter'
    Plug 'preservim/nerdtree'
    Plug 'junegunn/vim-plug'
    Plug 'godlygeek/tabular'
    if executable('ctags') || executable('exctags')
        Plug 'majutsushi/tagbar'
    endif
    if ! has('win32') && executable('curl')
        Plug 'echuraev/translate-shell.vim', { 'do': 'curl -o ~/bin/trans https://git.io/trans && chmod +x ~/bin/trans' }
    endif
    Plug 'mbbill/undotree'
    if executable('git')
        Plug 'tpope/vim-fugitive'
        Plug 'junegunn/gv.vim'
        Plug 'airblade/vim-gitgutter'
        Plug 'idanarye/vim-merginal'
    endif
    Plug 'vim-airline/vim-airline'
    Plug 'vim-airline/vim-airline-themes'
    Plug 'jeetsukumaran/vim-buffergator'
    Plug 'easymotion/vim-easymotion'
    Plug 'tpope/vim-eunuch'
    Plug 'terryma/vim-expand-region'
    Plug 'drmikehenry/vim-fontsize'
    Plug 'sheerun/vim-polyglot'
    Plug 'tpope/vim-repeat'
    Plug 'honza/vim-snippets'
    Plug 'tpope/vim-speeddating'
    Plug 'tpope/vim-surround'
    if has('python3')
        Plug 'vim-vdebug/vdebug', { 'for': [ 'php', 'python' ] }
    endif
    Plug 'idanarye/vim-vebugger', { 'for': [ 'c', 'cpp', 'java' ] }
    if has('win32')
        Plug 'Shougo/vimproc.vim'
    elseif executable('gmake')
        Plug 'Shougo/vimproc.vim', { 'do': 'gmake' }
    elseif executable('make')
        Plug 'Shougo/vimproc.vim', { 'do': 'make' }
    endif
    Plug 'vim-jp/vimdoc-ja'

    call plug#end()

    " colorscheme {{{2
    if count(getcompletion('', 'color'), 'OceanicNext') == 1
        colorscheme OceanicNext
        set background=dark
    endif

    " coc.nvim {{{2
    if s:is_plugged('coc.nvim')
        " if hidden is not set, TextEdit might fail.
        set hidden

        " Some servers have issues with backup files, see #649
        set nobackup
        set nowritebackup

        " Better display for messages
        set cmdheight=2

        " You will have bad experience for diagnostic messages when it's default 4000.
        set updatetime=300

        " don't give |ins-completion-menu| messages.
        set shortmess+=c

        " always show signcolumns
        set signcolumn=yes

        " Use tab for trigger completion with characters ahead and navigate.
        " Use command ':verbose imap <tab>' to make sure tab is not mapped by other plugin.
        inoremap <silent><expr> <TAB>
                    \ pumvisible() ? "\<C-n>" :
                    \ <SID>check_back_space() ? "\<TAB>" :
                    \ coc#refresh()
        inoremap <expr><S-TAB> pumvisible() ? "\<C-p>" : "\<C-h>"

        function! s:check_back_space() abort
            let col = col('.') - 1
            return !col || getline('.')[col - 1]  =~# '\s'
        endfunction

        " Use <c-space> to trigger completion.
        inoremap <silent><expr> <c-space> coc#refresh()

        " Use <cr> to confirm completion, `<C-g>u` means break undo chain at current position.
        " Coc only does snippet and additional edit on confirm.
        inoremap <expr> <cr> pumvisible() ? "\<C-y>" : "\<C-g>u\<CR>"
        " Or use `complete_info` if your vim support it, like:
        " inoremap <expr> <cr> complete_info()["selected"] != "-1" ? "\<C-y>" : "\<C-g>u\<CR>"

        " Use `[g` and `]g` to navigate diagnostics
        nmap <silent> [g <Plug>(coc-diagnostic-prev)
        nmap <silent> ]g <Plug>(coc-diagnostic-next)

        " Remap keys for gotos
        nmap <silent> gd <Plug>(coc-definition)
        nmap <silent> gy <Plug>(coc-type-definition)
        nmap <silent> gi <Plug>(coc-implementation)
        nmap <silent> gr <Plug>(coc-references)

        " Use K to show documentation in preview window
        nnoremap <silent> K :call <SID>show_documentation()<CR>

        function! s:show_documentation()
            if (index(['vim','help'], &filetype) >= 0)
                execute 'h '.expand('<cword>')
            else
                call CocAction('doHover')
            endif
        endfunction

        " Highlight symbol under cursor on CursorHold
        autocmd CursorHold * silent call CocActionAsync('highlight')

        " Remap for rename current word
        nmap <leader>rn <Plug>(coc-rename)

        " Remap for format selected region
        xmap <leader>f  <Plug>(coc-format-selected)
        nmap <leader>f  <Plug>(coc-format-selected)

        augroup mygroup
            autocmd!
            " Setup formatexpr specified filetype(s).
            autocmd FileType typescript,json setl formatexpr=CocAction('formatSelected')
            " Update signature help on jump placeholder
            autocmd User CocJumpPlaceholder call CocActionAsync('showSignatureHelp')
        augroup end

        " Remap for do codeAction of selected region, ex: `<leader>aap` for current paragraph
        xmap <leader>a  <Plug>(coc-codeaction-selected)
        nmap <leader>a  <Plug>(coc-codeaction-selected)

        " Remap for do codeAction of current line
        nmap <leader>ac  <Plug>(coc-codeaction)
        " Fix autofix problem of current line
        nmap <leader>qf  <Plug>(coc-fix-current)

        " Create mappings for function text object, requires document symbols feature of languageserver.
        xmap if <Plug>(coc-funcobj-i)
        xmap af <Plug>(coc-funcobj-a)
        omap if <Plug>(coc-funcobj-i)
        omap af <Plug>(coc-funcobj-a)

        " Use <C-d> for select selections ranges, needs server support, like: coc-tsserver, coc-python
        nmap <silent> <C-d> <Plug>(coc-range-select)
        xmap <silent> <C-d> <Plug>(coc-range-select)

        " Use `:Format` to format current buffer
        command! -nargs=0 Format :call CocAction('format')

        " Use `:Fold` to fold current buffer
        command! -nargs=? Fold :call     CocAction('fold', <f-args>)

        " use `:OR` for organize import of current buffer
        command! -nargs=0 OR   :call     CocAction('runCommand', 'editor.action.organizeImport')

        " Add status line support, for integration with other plugin, checkout `:h coc-status`
        set statusline^=%{coc#status()}%{get(b:,'coc_current_function','')}

        " Using CocList
        " Show all diagnostics
        nnoremap <silent> <space>a  :<C-u>CocList diagnostics<cr>
        " Manage extensions
        nnoremap <silent> <space>e  :<C-u>CocList extensions<cr>
        " Show commands
        nnoremap <silent> <space>c  :<C-u>CocList commands<cr>
        " Find symbol of current document
        nnoremap <silent> <space>o  :<C-u>CocList outline<cr>
        " Search workspace symbols
        nnoremap <silent> <space>s  :<C-u>CocList -I symbols<cr>
        " Do default action for next item.
        nnoremap <silent> <space>j  :<C-u>CocNext<CR>
        " Do default action for previous item.
        nnoremap <silent> <space>k  :<C-u>CocPrev<CR>
        " Resume latest coc list
        nnoremap <silent> <space>p  :<C-u>CocListResume<CR>


        " Use <C-l> for trigger snippet expand.
        imap <C-l> <Plug>(coc-snippets-expand)

        " Use <C-j> for select text for visual placeholder of snippet.
        vmap <C-j> <Plug>(coc-snippets-select)

        " Use <C-j> for jump to next placeholder, it's default of coc.nvim
        let g:coc_snippet_next = '<c-j>'

        " Use <C-k> for jump to previous placeholder, it's default of coc.nvim
        let g:coc_snippet_prev = '<c-k>'

        " Use <C-j> for both expand and jump (make expand higher priority.)
        imap <C-j> <Plug>(coc-snippets-expand-jump)
    endif

    " fzf.vim {{{2
    if s:is_plugged('fzf.vim')
        nnoremap <silent> <Leader>fb :Buffers<CR>
        nnoremap <silent> <Leader>ff :Files<CR>
        nnoremap <silent> <Leader>fh :History<CR>
        nnoremap <silent> <Leader>fl :BLines<CR>
    endif

    " markdown-preview.nvim {{{2
    if s:is_plugged('markdown-preview.nvim')
        let g:mkdp_auto_start = 1
        let g:mkdp_preview_options = { 'sync_scroll_type': 'relative' }
        let g:mkdp_page_title = '${name}'
    endif

    " nerdtree {{{2
    if s:is_plugged('nerdtree')
        nnoremap <Leader>n :NERDTreeToggle<CR>
        nnoremap <Leader>N :Cd<CR>:NERDTreeCWD<CR>
        let NERDTreeBookmarksFile = s:user_dir . '/NERDTreeBookmarks'
    endif

    " tagbar {{{2
    if s:is_plugged('tagbar')
        if executable('exctags')
            let g:tagbar_ctags_bin = 'exctags'
        elseif executable('ctags')
            let g:tagbar_ctags_bin = 'ctags'
        endif

        nnoremap <silent> <Leader>t :TagbarToggle<CR>
    endif

    " undotree {{{2
    if s:is_plugged('undotree')
        let g:undotree_WindowLayout = 3

        nnoremap <silent> <Leader>u :UndotreeToggle<CR>
    endif

    " vim-airline {{{2
    if s:is_plugged('vim-airline')
        let g:airline#extensions#tabline#enabled = 1
        let g:airline#extensions#tabline#show_buffers = 0
    endif

    " vim-buffergator {{{2
    if s:is_plugged('vim-buffergator')
        let g:buffergator_autoexpand_on_split = 0
        let g:buffergator_suppress_keymaps = 1
        let g:buffergator_viewport_split_policy = 'R'

        nnoremap <Leader>b :BuffergatorToggle<CR>
    endif

    " vim-easymotion {{{2
    if s:is_plugged('vim-easymotion')
        let g:EasyMotion_do_mapping = 0 " Disable default mappings

        " Jump to anywhere you want with minimal keystrokes, with just one key binding.
        " `s{char}{label}`
        nmap s <Plug>(easymotion-overwin-f)
        " or
        " `s{char}{char}{label}`
        " Need one more keystroke, but on average, it may be more comfortable.
        nmap s <Plug>(easymotion-overwin-f2)

        " Turn on case-insensitive feature
        let g:EasyMotion_smartcase = 1

        " JK motions: Line motions
        map <Leader>j <Plug>(easymotion-j)
        map <Leader>k <Plug>(easymotion-k)
    endif

    " vim-markdown {{{2
    if s:is_plugged('vim-markdown')
        let g:vim_markdown_folding_disabled = 1
    endif

    " vdebug {{{2
    if s:is_plugged('vdebug')
        if ! exists('g:vdebug_options')
            let g:vdebug_options = {}
        endif
        let g:vdebug_options.break_on_open = 0
    endif

    " vimproc.vim {{{2
    if s:is_plugged('vimproc.vim')
        if has('win32')
            let g:vimproc#download_windows_dll = 1
        endif
    endif
endif
